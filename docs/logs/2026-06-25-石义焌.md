# 2026-06-25 工作日志 - 石义焌

## 今日任务

1. 整理数据集格式
2. 编写数据处理脚本
3. 统一类别为 `falling_object`
4. 支持视频抽帧
5. 阅读算法 Pipeline 占位代码

## 完成结果

- 新增 `scripts/prepare_dataset.py`
- 支持 `images/` + `labels/` 转 YOLO 训练目录
- 自动划分 `train/val`
- 将所有 YOLO 标注类别统一为 `0 falling_object`
- 无标注图片生成空 label，保留负样本
- 支持可选视频抽帧
- 新增 `docs/dataset.md` 说明数据集目录、类别和使用命令
- 新增脚本测试 `tests/algorithm/test_prepare_dataset.py`
- 已在本机生成 `data/falling_object/` 训练目录结构

## 遇到的问题

天池高空抛物数据集需要登录下载；真实图片、视频和权重不能提交 Git。

## 解决办法

只提交数据处理脚本、目录说明和样例标注格式；真实图片、视频和权重继续放本地目录，不进 Git。

## 明日计划

1. 小规模训练 YOLOv11 首版模型
2. 输出训练日志和指标
3. 准备 `best.pt` 本地权重

## 相关截图或代码提交

- `scripts/prepare_dataset.py`
- `docs/dataset.md`
- `tests/algorithm/test_prepare_dataset.py`
