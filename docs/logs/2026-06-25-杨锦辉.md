# 2026-06-25 工作日志 - 杨锦辉

## 今日任务

1. 编写登录、系统状态、数据库初始化的测试用例
2. 产出测试用例表、接口测试记录
3. 验证登录和健康检查可执行

## 完成结果

### 接口手动测试

启动后端（uvicorn, 127.0.0.1:8000），用curl执行9项测试：

| 编号 | 测试项 | 结果 | 说明 |
|---|---|---|---|
| API-AUTH-001 | 正确账号登录 | ✅ | token 32位hex, username/role正确 |
| API-AUTH-002 | 错误密码 | ✅ | code:401, "账号或密码错误" |
| API-AUTH-003 | 无token访问 | ✅ | HTTP 401, 鉴权生效 |
| API-AUTH-004 | 空用户名 | ⚠️ | 返回401(预期400)，前端校验已覆盖，可接受 |
| API-AUTH-005 | 空密码 | ⚠️ | 同API-AUTH-004 |
| API-SYS-001 | 健康检查 | ✅ | status:"ok"+data.status:"running", 字段完整 |
| API-SYS-002 | 系统状态四维 | ✅ | backend/database/algorithm/device全部返回 |
| API-SYS-003 | 模型缺失检测 | ✅ | algorithm:"missing_model", model_loaded:false |
| API-SYS-004 | 无token访问system | ✅ | HTTP 401, 鉴权生效 |

### 自动化测试验证

陈磊的53个pytest用例全部通过（0.63s）：

```
tests/backend/test_auth.py        —  8 passed (登录成功/失败/边界)
tests/backend/test_auth_core.py   — 17 passed (token生成/校验/过期)
tests/backend/test_health.py      —  5 passed (格式/字段/时间)
tests/backend/test_system.py      —  9 passed (鉴权/四维/设备)
tests/backend/test_database.py    — 14 passed (DDL/约束/幂等/默认值)
```

### 数据库验证

通过DB Browser for SQLite确认：
- 5张表全部创建：`system_settings`, `video_tasks`, `events`, `detection_results`, `tracking_results`
- `system_settings`默认行已插入，6个参数与需求一致（0.35, 0.7, 80, 5, 0.7, 10）
- 其余4张数据表为空（正确，尚无视频任务）

### 测试记录文档

产出 `tests/integration/test-records/2026-06-25-接口测试记录.md`，包含每条用例的curl命令、实际响应JSON、预期对比。

## 测试发现的问题

### 问题 #001：空用户名/密码返回401 (P2)

API设计约定空参数返回400，实际返回401 "账号或密码错误"。与陈磊确认：前端表单已有必填校验，用户不会直接调用API发空值，当前行为不影响实际使用。标记为"不修复，记录备查"。

### 问题 #002：GPU场景待补测 (P3)

本机无GPU，`algorithm.status:"ready"`（模型就绪+GPU可用）的场景未验证。已记录到问题清单，待6/28集成时在罗龙飞(RTX3050)机器上补测。

## 与开发对接情况

- **陈磊**：确认空参数401可接受；53个pytest全部通过，接口质量良好；明天上传接口就绪后开始第二轮测试
- **刘康**：确认登录接口字段名 `username`/`password`（非`account`），前端表单绑定需一致；health接口顶层`status:"ok"`和`data.status:"running"`两个字段区别已说明
- **罗龙飞**：站会同步今日测试结果，无阻塞问题，6/26进入上传接口测试

## 明日计划

1. 测试视频上传接口（`POST /api/videos/upload`）
   - 正常MP4上传
   - 非视频文件拒绝
   - 损坏视频处理
2. 准备测试视频素材（需1-2段正常抛物视频 + 1个txt伪装视频 + 1个损坏视频）
3. 产出上传测试记录
4. 若上传接口就绪早，提前验证任务查询接口

## 相关记录或截图

- [2026-06-25 接口测试记录](../tests/integration/test-records/2026-06-25-接口测试记录.md)
- [集成问题清单](../tests/integration/issues/集成问题清单.md)（新增#001、#002）
