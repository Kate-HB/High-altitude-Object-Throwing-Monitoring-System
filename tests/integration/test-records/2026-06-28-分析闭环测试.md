# 2026-06-28 分析闭环联调测试

## 测试目标

验证上传→分析→事件入库完整链路，确认ROI、任务进度、事件查询功能联调通过。

## 测试环境

- 后端: uvicorn, http://localhost:8000
- 数据库: SQLite (app.db)
- 算法: 占位符 (返回 status="not_ready")
- 测试视频: 30帧 MP4 (OpenCV生成)

## 测试结果汇总

| 测试项 | 结果 | 备注 |
|---|---|---|
| 登录获取token | ✅ 通过 | admin/admin123 返回32位token |
| 上传MP4视频 | ✅ 通过 | 返回task_id, total_frames=30 |
| 查询任务详情 | ✅ 通过 | 返回完整字段(status=pending) |
| 启动分析(带ROI) | ✅ 通过 | 返回status=running |
| 轮询任务状态变化 | ✅ 通过 | pending→running→failed |
| 算法占位返回not_ready | ✅ 通过 | error_message="Detection model is not loaded" |
| 查询事件(空) | ✅ 通过 | events=[], count=0 |
| 后端pytest全量 | ✅ 82/82 通过 | 测试时间 1.37s |

## 详细测试记录

### TC-INT-001: 上传→分析→状态查询闭环

```
步骤1: POST /api/auth/login → token
步骤2: POST /api/videos/upload (file=@test_real.mp4) → task_id=29
步骤3: GET /api/tasks/29 → status=pending, total_frames=30
步骤4: POST /api/tasks/29/analyze (roi_x=50,roi_y=50,roi_width=300,roi_height=200) → status=running
步骤5: GET /api/tasks/29 (3秒后) → status=failed, error_message="Detection model is not loaded"
```

### TC-INT-002: ROI参数传递

```
步骤1: POST /api/tasks/29/analyze 传入ROI (50,50,300,200)
步骤2: GET /api/tasks/29 → roi_x=0, roi_y=0, roi_width=null, roi_height=null
结果: ❌ ROI未入库 — 见问题 #003
```

### TC-INT-003: 事件查询(无事件)

```
步骤1: GET /api/events?task_id=29
步骤2: 返回 {"events":[], "count":0}
结果: ✅ 接口正常，空事件返回格式正确
```

## 数据库验证

```
Task #29: status=failed, ROI=(0,0,None,None), err=Detection model is not loaded
Events: 0条 (算法未产出)
```

## 阻塞项

1. **ROI未入库** (#003, P1): 分析时ROI参数不写入video_tasks表
2. **算法未就绪**: 占位符返回not_ready，事件写入逻辑未触发，需石义焌交付后验证

## 结论

- 主链路（上传→创建任务→启动分析→状态变化）畅通
- ROI参数传递存在缺陷，需陈磊修复
- 事件入库、检测结果等链路等待算法真实产出后验证
- 82个pytest全量通过，无回归问题
