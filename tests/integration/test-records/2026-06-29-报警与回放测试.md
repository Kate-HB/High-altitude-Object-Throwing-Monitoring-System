# 2026-06-29 报警与回放功能测试

## 测试目标

验证事件确认/误报标记/状态更新API、数据库一致性、文件访问安全、历史回放功能。

## 测试环境

- 后端: uvicorn, http://localhost:8000
- 数据库: SQLite (system.db)
- 算法: YOLOv11 + IOU Tracker (已集成)
- 测试事件: id=208,209 (来自task_52算法分析)

## 测试结果汇总

| # | 测试项 | 结果 | 备注 |
|---|---|---|---|
| TC1 | 事件详情（含detections+tracks） | ✅ | id=208: 144 detections + 331 tracks |
| TC2 | 事件确认（confirmed） | ✅ | API 200, DB status=confirmed |
| TC3 | 事件误报（false_alarm） | ✅ | API 200, DB status=false_alarm |
| TC4 | 恢复未确认（unconfirmed） | ✅ | API 200 |
| TC5 | 非法状态值拒绝 | ✅ | status=invalid → 400 |
| TC6 | 不存在事件返回404 | ✅ | id=99999 → 404 |
| TC7 | 文件访问-路径穿越拦截 | ✅ | path=../.env → 403 |
| TC8 | 未鉴权访问拒绝 | ✅ | 无token → 401 |
| TC9 | pytest全量回归 | ✅ | 82/82 passed |
| TC10 | Playwright E2E | ✅ | 20/20 passed |
| TC11 | DB 5表完整性 | ✅ | system_settings(1)+video_tasks(60)+events+detections+tracking |
| TC12 | API→DB状态一致性 | ✅ | confirmed/false_alarm写入后DB即时反映 |

## 详细测试记录

### TC1: 事件详情接口

```
GET /api/events/208
→ 200 { id, track_id, status, confidence, detections[], tracks[] }
detections: 144条 (frame_id, bbox, confidence)
tracks: 331条 (frame_id, center_x/y, bbox)
```

### TC2-4: 事件状态更新全流转

```
PATCH /api/events/208/status {status:"confirmed"}  → 200
DB: status=confirmed ✅

PATCH /api/events/208/status {status:"false_alarm"} → 200
DB: status=false_alarm ✅

PATCH /api/events/208/status {status:"unconfirmed"} → 200
DB: status=unconfirmed ✅
```

### TC5-6: 异常边界

```
PATCH /api/events/208/status {status:"invalid"}     → 400 "无效状态值"
PATCH /api/events/99999/status {status:"confirmed"}  → 404 "事件不存在"
```

### TC7: 文件访问安全

```
GET /api/files?path=../.env        → 403 "路径非法或越界"
GET /api/files?path=/etc/passwd    → 403
GET /api/files?path=               → 403
```

### TC8: 鉴权验证

```
GET /api/events (无token)          → 401
GET /api/events (无效token)        → 401
```

### TC9: pytest全量回归

```
82 passed in 1.05s
- test_auth_core.py: 20 passed
- test_database.py: 10 passed
- test_events.py: 15 passed
- test_health.py: 5 passed
- test_system.py: 9 passed
- test_videos.py: 23 passed
```

### TC10: Playwright前端E2E

```
20 passed in 12.9s
- login.spec.js: 5 tests
- home.spec.js: 5 tests
- video-analysis.spec.js: 10 tests
```

### TC11-12: 数据库一致性

```
events表: 外键→video_tasks正确
detection_results: 外键→video_tasks正确
tracking_results: 外键→video_tasks正确
状态变更后updated_at自动更新
```

## 已修复问题回顾

| # | 描述 | 状态 |
|---|---|---|
| #003 | ROI未入库 | ✅ 6/29修复 |
| #004 | 代码重复 | ✅ 6/29修复 |
| #005 | frames回写 | ✅ 6/29修复 |
| 文件安全 | files.py白名单+防穿越 | ✅ 6/29新增 |

## 已知限制

- 报警弹窗/声音：前端尚未实现弹窗组件和报警音效
- 事件数少（2条）：需更多真实测试视频积累事件数据
- 截图/视频文件：依赖算法pipeline产出，当前测试数据为空
